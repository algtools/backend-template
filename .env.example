# Copy to .env.local (gitignored) for local sentry-cli uploads after deploy.
# Create a token in Sentry: Settings → Auth Tokens (scopes: project:releases, org:read).
SENTRY_ORG=algenium
SENTRY_PROJECT=backend-template
SENTRY_AUTH_TOKEN=sntrys_YOUR_TOKEN_HERE
